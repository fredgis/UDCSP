<policies>
  <inbound>
    <base />
    <!-- The API-level base validates the JWT, rejects body subject mismatches and sets the trusted citizen header. -->
    <set-variable name="reqBody" value="@{
      var raw = context.Request.Body == null ? &quot;&quot; : context.Request.Body.As&lt;string&gt;(preserveContent: true);
      return string.IsNullOrEmpty(raw) ? new JObject() : JObject.Parse(raw);
    }" />
    <return-response>
      <set-status code="202" reason="Accepted" />
      <set-header name="Access-Control-Allow-Origin" exists-action="override"><value>@{
        var origin = context.Request.Headers.GetValueOrDefault("Origin","");
        return origin == "https://udcsp.fredgis.com"
            || origin == "https://icy-dune-01c23d903.7.azurestaticapps.net"
            || origin == "http://localhost:3000"
            || origin == "http://localhost:5173"
          ? origin
          : "https://udcsp.fredgis.com";
      }</value></set-header>
      <set-header name="Access-Control-Allow-Credentials" exists-action="override"><value>true</value></set-header>
      <set-header name="Vary" exists-action="override"><value>Origin</value></set-header>
      <set-header name="content-type" exists-action="override">
        <value>application/json</value>
      </set-header>
      <set-body>@{
        var body = (JObject)context.Variables["reqBody"];
        var country = (string)(body["country"] ?? "dk");
        var citizen = (string)context.Variables["citizenUpn"];
        var certId = "ERASURE-" + country.ToUpper() + "-" + DateTime.UtcNow.ToString("yyyyMMddHHmmss") + "-" + Guid.NewGuid().ToString("N").Substring(0, 8).ToUpper();
        var eta = DateTime.UtcNow.AddDays(30).ToString("yyyy-MM-dd");
        var resp = new JObject {
          ["status"] = "accepted",
          ["certificateId"] = certId,
          ["estimatedCompletionDate"] = eta,
          ["country"] = country,
          ["citizenUpn"] = citizen,
          ["receivedAt"] = DateTime.UtcNow.ToString("o"),
          ["zones"] = new JArray("dataverse", "foundry-conversations", "appinsights", "blob-lake", "purview-lineage"),
          ["note"] = "Erasure cascade orchestrated by Microsoft Priva. Audit-only metadata required by law (GDPR art. 17(3)(b)/(e)) is retained."
        };
        return resp.ToString();
      }</set-body>
    </return-response>
  </inbound>
  <backend><base /></backend>
  <outbound><base /></outbound>
  <on-error><base /></on-error>
</policies>
