# Power BI Thin Model Layout

- `semantic-model/` — DirectLake connection to `citizen-services-semantic-model.tmdl`.
- `reports/` — PBIP report definitions imported into country and shared analytics workspaces.
- `report-pages.md` — page inventory and intended personas.
