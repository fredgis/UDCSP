// TODO: case-study scaffold. B2C OIDC test bypass via token env vars.
import { Page, expect } from '@playwright/test';
export async function signInWithB2CTestToken(page:Page,country:'DK'|'SE'|'NO',personaId:string){ const token=process.env[`UDCSP_${country}_B2C_TEST_TOKEN`]??process.env.UDCSP_TEST_TOKEN??'scaffold-token'; await page.addInitScript(([t,p])=>{localStorage.setItem('udcsp:test-token',t); localStorage.setItem('udcsp:persona-id',p);},[token,personaId]); await page.goto(`/login/b2c/${country}?testBypass=true`); await expect(page.getByTestId('signed-in-banner')).toContainText(personaId); return token; }
