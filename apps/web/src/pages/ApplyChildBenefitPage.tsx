import { preAssessEligibility } from '../api/eligibility';
export function ApplyChildBenefitPage() { return <form onSubmit={e => { e.preventDefault(); void preAssessEligibility(Object.fromEntries(new FormData(e.currentTarget))); }}><h1>Child benefit</h1><label htmlFor="children">Number of children</label><input id="children" name="children" type="number" min="1" required /><label htmlFor="residence">Residence country</label><select id="residence" name="residence" required><option value="dk">Denmark</option><option value="se">Sweden</option><option value="no">Norway</option></select><button>Check and apply</button></form>; }
