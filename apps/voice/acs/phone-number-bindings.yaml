# Generated by apps/voice/scripts/Bind-AcsNumber.ps1 — see docs/biz/voice.md §9.4
# Each entry binds a regulator-issued PSTN number to the country ACS resource
# and to the voice orchestrator's IncomingCall webhook. Install-Voice reads
# this file at install time so the operator does not have to repeat the
# bind step on every redeploy.
bindings: []
